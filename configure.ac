# Generated by invil v0.2.8
AC_INIT([canvil], [0.1.2], [jgabaut@github.com])
AM_INIT_AUTOMAKE([foreign -Wall])
AC_CANONICAL_HOST
build_linux=no
build_windows=no
build_mac=no
echo "Host os:  $host_os"

AC_ARG_ENABLE([debug],  [AS_HELP_STRING([--enable-debug], [Enable debug build])],  [enable_debug=$enableval],  [enable_debug=no])
AM_CONDITIONAL([DEBUG_BUILD], [test "$enable_debug" = "yes"])
AC_ARG_ENABLE([nogit],  [AS_HELP_STRING([--enable-nogit], [Enable nogit build])],  [enable_nogit=$enableval],  [enable_nogit=no])
case "${host_os}" in
	mingw*)
		echo "Building for mingw32: [$host_cpu-$host_vendor-$host_os]"
		build_windows=yes
        enable_nogit=yes
		AC_SUBST([CANVIL_CFLAGS], ["-I/usr/x86_64-w64-mingw32/include -static -fstack-protector -DWINDOWS_BUILD"])
		AC_SUBST([CANVIL_LDFLAGS], ["-L/usr/x86_64-w64-mingw32/lib -L ./tomlc17/src -ltomlc17 -larchive -lzs -liconv -lcharset -lpcre2-posix -lpcre2-8"])
		AC_SUBST([CCOMP], ["/usr/bin/x86_64-w64-mingw32-gcc"])
		AC_SUBST([OS], ["w64-mingw32"])
		AC_SUBST([TARGET], ["canvil.exe"])
	;;
	darwin*)
		build_mac=yes
		echo "Building for macos: [$host_cpu-$host_vendor-$host_os]"
		AC_SUBST([CANVIL_CFLAGS], ["-I/opt/homebrew/opt/libgit2/include"])
		AC_SUBST([CANVIL_LDFLAGS], ["-L/opt/homebrew/opt/libgit2/lib -L ./tomlc17/src -ltomlc17 -lgit2 -larchive"])
		AC_SUBST([OS], ["darwin"])
		AC_SUBST([TARGET], ["canvil"])
	;;
	linux*)
		echo "Building for Linux: [$host_cpu-$host_vendor-$host_os]"
		build_linux=yes
		AC_SUBST([CANVIL_CFLAGS], [""])
		AC_SUBST([CANVIL_LDFLAGS], ["-L ./tomlc17/src -ltomlc17 -lgit2 -larchive"])
		AC_SUBST([OS], ["Linux"])
		AC_SUBST([TARGET], ["canvil"])
	;;
esac

AM_CONDITIONAL([NOGIT_BUILD], [test "$enable_nogit" = "yes"])
AM_CONDITIONAL([DARWIN_BUILD], [test "$build_mac" = "yes"])
AM_CONDITIONAL([WINDOWS_BUILD], [test "$build_windows" = "yes"])
AM_CONDITIONAL([LINUX_BUILD], [test "$build_linux" = "yes"])

AC_ARG_VAR([VERSION], [Version number])
if test -z "$VERSION"; then
  VERSION="0.1.2"
fi
AC_DEFINE_UNQUOTED([VERSION], ["$VERSION"], [Version number])
AC_CHECK_PROGS([CCOMP], [gcc clang])
AC_CHECK_HEADERS([stdio.h git2.h])
AC_CHECK_FUNCS([malloc calloc])
AC_CONFIG_FILES([Makefile])
AC_OUTPUT
